[project]
name = "sml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.13"
dependencies = [
    "lm-eval[api]>=0.4.12",
    "numpy>=2.4.6",
    "sentencepiece>=0.2.1",
    "torch>=2.12.0",
    "zstandard>=0.25.0",
]
